import createElement from "./createElement.js"

export default function errorContainer () {
    const errorContainer = createElement("div", "col-span-1 sm:col-span-2 xl:col-span-4")

    const errorMessage = createElement("p", "", "Could not load content, with the following error-message:");

    errorText = createElement("p", "", )
}